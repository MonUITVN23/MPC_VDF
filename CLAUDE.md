 SYSTEM PROMPT & PROJECT CONTEXT: Hybrid VDF-MPC Protocol for Multi-Bridge Cross-chain DAOs

## 1. Project Overview & Academic Goals
You are assisting in the development of a Proof-of-Concept (PoC) for a highly secure, decentralized, and cross-chain Verifiable Random Function (VRF) protocol [1]. This system solves the "Randomness Paradox" in distributed networks, specifically targeting multi-chain Decentralized Autonomous Organizations (DAOs) [2, 3].

The system implements a **Defense-in-Depth (Dual-layer + ZK)** security model:
1.  **Multi-Party Computation (MPC):** Prevents **Input Bias**. A collective seed is generated by a committee threshold, ensuring no single entity can manipulate the initial randomness [4].
2.  **Verifiable Delay Function (VDF):** Prevents **Premature Prediction (Front-running)**. Forces a sequential time delay $T$ before the final result is known, making the MPC committee's early knowledge useless [5, 6].
3.  **ZK (Halo2/IPA):** Prevents **Curve Mismatch Issues**. Off-chain proof of cryptographic binding is verified on-chain via BN254 precompiles [18].

### Core Philosophies (CRITICAL)
-   **Bridge-Agnostic Verification:** Bridges are treated as transport layers only. The destination application must never trust payloads blindly and must validate cryptographic conditions before finalization [7].
-   **Optimistic Verification:** To avoid high gas costs for mandatory on-chain VDF checks, destination contracts use an optimistic queue. Expensive verification runs only when challenged during a challenge window [8, 9].
-   **Keep Research Direction Stable:** Preserve the MPC + VDF thesis and benchmarking methodology; update only implementation details that are outdated versus the current repo architecture.

---

## 2. Current Project Context Snapshot (2026-03-25)

### On-chain architecture (Completed)
-   Source side is **Router/Adapter-based multi-bridge**:
    -   `contracts/src/RandomRouter.sol`
    -   `contracts/src/interfaces/IBridgeAdapter.sol`
    -   `contracts/src/adapters/AxelarAdapter.sol`
    -   `contracts/src/adapters/LayerZeroAdapter.sol`
    -   `contracts/src/adapters/WormholeAdapter.sol`
-   Receiver side: `RandomReceiver.sol` handles optimistic queueing and challenge/finalize flow. It verifies Halo2 proof bytes and public signals binding (`payload_hash`, `pk_hash`, `request_id`).

### ZK Architecture (Halo2 native Rust)
-   Moved away from zkVM-heavy proving due to hardware constraints.
-   Main proving path is Halo2 native in Rust (`off-chain/halo2_prover/`), integrated directly with `crypto_engine` and `network_module`.
-   On-chain verification is handled by `Halo2Verifier.sol` through `ITransparentVerifier` abstraction.

### Off-chain status (In Progress)
-   `network_module` and `crypto_engine` are split.
-   Multi-bridge failover is implemented in `bridges.rs`.
-   **Current:** Rust pipeline already calls Halo2 prover directly; no TS `prove.js` dependency in the main flow.

---

## 3. Technology Stack
When generating code, strictly adhere to the following stack [10]:
-   **Smart Contracts:** Solidity (Hardhat framework).
-   **ZK:** Halo2/IPA prover in Rust, transparent/no trusted setup.
-   **Off-chain Node / Crypto Engine:** Rust with Node.js interop for proving.
-   **Rust Crypto Crates:**
    -   `vdf-rs` — **MANDATORY: Imaginary Quadratic Class Groups (IQCG)**. Do NOT use RSA groups [15].
    -   `bls-signatures` — for threshold signatures / DKG.
-   **Blockchain Networks:** Ethereum Sepolia (source) and Polygon Amoy/Sepolia (destination).
-   **Cross-chain Interoperability:** Axelar / LayerZero / Wormhole adapters.

### MPC Threshold Configuration (PoC Defaults)
For PoC and Unit Testing:
| Config | Total Nodes (n) | Threshold (t) | Description |
|--------|-----------------|---------------|-------------|
| **Small** | 4 | 3 | 3-of-4 threshold (default for local testing) |
| **Medium** | 10 | 7 | 7-of-10 threshold (for stress testing) |

---

## 4. End-to-End Workflow & Architecture (Halo2 ZK Flow)
Implement this sequence and avoid redundant cross-chain messages:

1.  **Request:** User calls `requestRandomness(seed_user)` on `RandomRouter.sol`. Contract emits `LogRequest`.
2.  **MPC Generation:** Committee derives `seed_collective` and aggregate BLS12-381 signature $\sigma$.
3.  **VDF Evaluation:** Compute $y = x^{2^T} \pmod \Delta$ and proof $\pi$ from `seed_collective`.
4.  **ZK Proving:** Off-chain node verifies BLS signature locally, then generates a Halo2 proof binding cryptographic data to the `payload_hash`.
5.  **Relay via Router+Adapter:** Executor calls `relayVDFPayload(..., zkProofData, zkPublicSignals)` on router; router dispatches payload.
6.  **Destination Optimistic Handling:** Receiver verifies the Halo2 proof (`zkVerifier.verifyProof`), binds public signals to the payload hash, enqueues result, and opens challenge window.
7.  **Challenge/Finalize:** Anyone can challenge during the window by running the VDF verify on-chain; if unchallenged, finalize randomness.

---

## 5. Mathematical Foundations (Rust Crypto Engine)
When writing off-chain Rust logic, apply:
-   **VDF Evaluation:** Strictly sequential squaring; no parallel shortcuts [15].
    -   $y = x^{2^T} \pmod \Delta$ where $\Delta$ is a large negative prime discriminant.
-   **Proof Generation (Wesolowski):** Let $2^T = q \cdot \ell + r$ [16].
    -   $\pi = x^q \pmod \Delta$.

---

## 6. Strict Coding Guidelines (Rules for AI Generation)

### A. Solidity (Smart Contracts)
-   **Gas-sensitive VDF Verify:** For heavy modular exponentiation, prefer precompile-based paths (`0x05`) over naive big-int loops [17].
-   **ZK Verification:** Maintain `ITransparentVerifier` abstraction in the receiver and wire to `Halo2Verifier`.

### B. Cross-chain Bridge Implementation
-   **Adapters:** Implemented behind common `IBridgeAdapter`. Do not hardwire protocol-specific business logic into router core.

### C. Rust (Off-chain Nodes)
-   **Modular Design:** `network_module` handles routing/telemetry; `crypto_engine` handles math.
-   **ZK Pipeline Integration:** Prefer direct Rust-native Halo2 proving APIs and structured proof serialization for relay payloads.

---

## 7. Benchmarking & Data Collection (Research Paper)
Automated scripts/tests should continue logging key metrics [20-22]:
-   **E2E Latency:** $L = t_{request} + t_{MPC} + t_{VDF} + t_{ZKProve} + t_{Relay} + t_{Verify}$
    - Log $t_{ZKProve}$ in E2E CSV metrics as `t3_5_zkprove_ms`.
-   **Prover Time:** Compare Tier 1 (Commitment) vs Tier 2 (Full Pairing).
-   **Gas Logging:** Compare gas costs: no verify vs Halo2 verify.
-   **Multi-bridge reporting:** Include bridge identity (`bridge_name`) and latency metadata.

---

## 8. Non-Negotiable Direction Guardrails
-   Do not remove MPC + VDF dual-security thesis.
-   Do not revert architecture to single hardcoded bridge.
-   Do not remove the ZK-SNARK tier methodology (it solves the BN254 vs BLS12-381 curve mismatch).
-   Do not compromise reproducible benchmarking required for paper outputs.